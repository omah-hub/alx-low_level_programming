#include <stdio.h>
/**
 *main- Serves as entry point for program
 *
 * Return: By default returns Zero if no erros otherwise returns non-zero value.
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
